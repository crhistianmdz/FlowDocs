# Shared Tasks — TaskBoard

This folder tracks **cross-package** epic-level work that spans multiple packages (web + mobile + api).

For package-specific HUs, see `packages/<pkg>/docs/tasks/`.

## Template
See `docs/tasks/TEMPLATE.md` (root) or copy from `packages/api/docs/tasks/`.

## Active shared work
- (Empty — when a feature needs coordination across 3+ packages, log it here.)